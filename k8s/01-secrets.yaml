apiVersion: v1
kind: Secret
metadata:
  name: nexus-secrets
  namespace: nexusreview
type: Opaque
stringData:
  # 注意：在真实的生产环境中，请通过 kubectl create secret 或外部密钥管理系统注入
  # 此处作为模板提供，核心 API-KEY 应来自 GitHub Secrets 或环境变量
  API_KEY: "your-api-key-here"
  MYSQL_ROOT_PASSWORD: "root"
  REDIS_PASSWORD: "123321"
